# === Google Cloud Platform ===
# ID do seu projeto GCP
GCP_PROJECT_ID=

# Nome do bucket do Cloud Storage (deve já existir ou ser criado antes de rodar a ingestão)
GCS_BUCKET_NAME=

# Nome do dataset do BigQuery (criado automaticamente na primeira execução se não existir)
BQ_DATASET=energy_quality

# === LLM / Embeddings (Google AI Studio -- cota gratuita diária) ===
GOOGLE_AI_STUDIO_API_KEY=
